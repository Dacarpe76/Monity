name: monity
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0 # O la última versión compatible
  provider: ^6.1.2 # O la última versión compatible
  hive: ^2.0.0
  hive_flutter: ^1.1.0
  path_provider: ^2.0.15
  cupertino_icons: ^1.0.8
  uuid: ^4.4.0 # O la última versión disponible
  freezed_annotation: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.0
  hive_generator: ^1.1.0
  flutter_lints: ^5.0.0
  freezed: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/screens/donut.jpg